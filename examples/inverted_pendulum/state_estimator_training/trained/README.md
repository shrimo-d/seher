# Trained state estimator

Running `state_estimator.py --mode train` with its default model path writes
`metadata.json` and `weights.msgpack` into this directory. Those generated
checkpoint files are intentionally ignored by Git.
